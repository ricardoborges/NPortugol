funcao indice()

fim

funcao calcular(valor)
    variavel resultado, visao
    
    resultado = fatorial(valor)
    
    visao = "resultado"
    
fim

funcao fatorial(x)

    se x == 0 retorne 1 fim
    
    retorne fatorial(x - 1) * x
fim