função principal()
  variável vetor 
 
  vetor = {8;2;7;3;6;4;5;0;1}
  
  ordenar(vetor)
  
  imprimaVetor(vetor)
fim

função ordenar(v)
  variável i, j, t, tam

  tam = v.tamanho
  
  para t = 1 até tam
    para i = 1 até tam
        j = i-1
        se v[j] > v[i] então
          aux = v[i]
          v[i] = v[j]
          v[j] = aux         
        fim
    fim
  fim
fim